let arr = [2, 'true', 15, 27, 36, 48];
alert(arr.length) //кол-во элементов массива
arr.forEach((item, index, array) => alert(item, index, array)); //вызов каждого элемента в порядке возрастания

